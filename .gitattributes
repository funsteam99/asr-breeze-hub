# These run on Linux. A CRLF line ending turns the shebang into `bash\r`, which
# fails with a confusing "no such file or directory". Force LF regardless of
# what platform the commit came from.
*.sh text eol=lf
*.py text eol=lf
*.template text eol=lf
*.json text eol=lf
.env.example text eol=lf
